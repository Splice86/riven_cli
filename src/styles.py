"""Styling definitions for Riven CLI output.

Re-exports from heading.py for backwards compatibility.
"""

from src.heading import (
    WHITE, RED, GREEN, YELLOW, CYAN, MAGENTA,
    GREY, DIM, RESET, BOLD,
    SECTIONS,
    section_header, section_content, truncate_output,
    MAX_LINES, MAX_LINE_LENGTH,
)
